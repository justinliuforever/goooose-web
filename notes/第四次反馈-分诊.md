# 第四次反馈 · 分诊（2026-07-12）

来源：`~/Downloads/Singulariy 7.12 第四次反馈`。状态基于当前 main（已含 PR#1/#2，worker 20260712.1）逐条核实，非仅凭同事口述。

图例：✅ 已做 · 🟡 部分 · ❌ 没做 · ❓ 是问题（需解答，非改代码）

---

## 前言（三个总体问题）

| # | 内容 | 状态 | 依据 / 说明 |
|---|---|---|---|
| P0 | clone 本地改 + merge 会不会摧毁 justin 的 origin | ❓ | 不会。PR 只在分支上；我们已干净合并 2 个 PR 验证过。同事按 fork→PR 流程完全安全 |
| P1 | 所有 SOP/output 是 md，网页是没渲染的 plain text，能否渲染 | 🟡 | **SOP 已渲染 markdown**（`components/markdown.tsx` react-markdown+gfm，SOP 卡片在用）。脚本是 plain `<pre>`（脚本是 `[HOOK]` 标记不是 markdown，渲染意义不大）。Bible/选题分析待抽查。反馈对 SOP 已过时 |
| P2 | 全站前端用 taste skill 规划布局 + 一键 dark/light 切换 | ❌ | 大工程。dark mode 需全站 token 化 + 切换控件；布局美化需设计方向。**建 workflow 候选** |

## 一、Clerk（同事称 1-7 已做，2 个 issue 未做）

| # | 内容 | 状态 | 依据 |
|---|---|---|---|
| 0 | 手机端 xhslink.com 短链不识别 | ❌ | 全库无 xhslink 处理。**我已调研**：服务端展开重定向→提 user id，落 `xhs.ts`（validators.ts 是浏览器模块放不了）。展开后 URL 常自带 xsec_token，与 #3 同一条管道 |
| 1 | 单条分析覆盖问题 + 单条/爆款拆解融合 + 单条补 3 节 + 保留钩子 | 🟡 | **补 3 节 ✅**（PR#2 给 buildHottestSopPrompt 加了脚本结构蓝图/讲故事框架/留人机制，单条拆解走同一 prompt，已实测产出）。**覆盖问题 ✅**（单条 SOP 按 videoId partial-unique，重析只替换该条，不动账号 SOP；页面单独列出带标题标签）。**「融合成一个功能」🟡**：单条(single_video) 与账号爆款(hottest) 仍是两个 sopType，UI 未合并为单一概念 |
| 2 | 爆款拆解 SOP 归属不清、加 title 小标签 | ✅ | PR#2：SopCard 📄 源帖子标题标签（sourceVideoTitle）|
| 3 | 点标题回跳原帖 404 | ❌ | 存库 URL 硬拼 `explore/<id>` 不带 xsec_token（xhs.ts:331）。**我已调研**：detail 端点响应里有 token（`mini_program_info.webpage_url`），实测拼上即通。建议点击时懒解析跳转路由（历史 295 条零回填修好） |
| 4 | 从头分析 vs 仅新视频的逻辑 | ❓ | **已是想要的行为**。仅新视频(incremental)=跳过已析、只跑新的；SOP 始终从全账号视频（新+旧缓存摘要）重新生成整份总 SOP（不是把旧 SOP 文本当 input，是全部 per-video 摘要→新 SOP）。从头分析(overwrite)=重跑选中批次。→ 同事想要的「只跑未分析」已存在，选「仅新视频」即可。纯解答 |
| 5 | 分析完不刷新 | ✅ | PR#2：router.refresh + 系列面板刷新 + 5s 周期刷新 |
| 6 | 封面手册显示视频标题→标题；SOP 显示「视频x」→「帖子x（图文/视频）」 | ✅ | PR#2：diagnostic 用真实标题；渲染层标注「帖子N（图文/视频）」，实测误称从 73→2 |
| 7 | 「x's profile」→「x的账号画像」 | 🟡 | **Clerk 页 ✅**（cleanProfileName + 账号画像）。**Muse 账号选择器 ❌ 仍显示 's profile**（muse-start-sheet.tsx:180 用原始 c.name 没清洗）——PR#2 漏了 Muse 侧 |

## 二、Bible（未做）

| # | 内容 | 状态 | 依据 |
|---|---|---|---|
| 1 | 导入生成简报的标准（短→总结给框架；长→原样保留）；疑问：写稿会用到 bible 全部内容吗 | ❓ | 部分需解答。锚点圣经 9 章节，下游 `selectBibleSections` **按需取节**：写稿只拿人设/方法论章节，**不接触 FACT_SHEET 事实类**（v0.6 防张冠李戴）。→ 写稿不会「用到全部」，是选择性取用。短/长内容处理差异需再验 importBible 逻辑 |
| 2 | 「我的账号」加账号知识库：导入背景/产品/人物，muse+poet 引用 | ❌ | 新功能。跨层：schema（知识库表）+ 导入管线（复用 bible import 的 docTranscribe）+ muse/poet prompt 注入点。**建 workflow 候选（架构设计）** |

## 三、Muse（未做）

| # | 内容 | 状态 | 依据 |
|---|---|---|---|
| 1 | 巡视增 3 功能：①方向自定义 ②选全账号 SOP 或指定单条爆款 SOP ③指定单条/多条链接拉取 | ❌ | 中型功能。落 muse-start-sheet + generateIdeas 参数 + prompt。②依赖 Clerk #1 的单条爆款 SOP 已可被指定 |
| 2 | 待处理呈现不清晰→按来源视频 mind-map（三级：来源/选题+采用忽略/展开事实数据等） | ❌ | 当前按 run 分组平铺（muse page.tsx newestRun/earlierRun）。要改成来源视频分组的思维导图交互。**建 workflow 候选（UX 设计）** |

## 四、Poet（未做）

| # | 内容 | 状态 | 依据 |
|---|---|---|---|
| 1 | 已生成脚本没名字/归类不清/与选题分开→放到该选题下面 | ❌ | UI 重组。脚本已有 ideaId/customTopicId 外键，可按选题聚合展示 |
| 2 | 写稿时绑定对标 SOP：全账号 SOP 或指定单条爆款 SOP | ❌ | 中型功能。与 Muse #1② 同构（同一「选 SOP」组件可复用）|

---

## 分诊汇总（按处理方式）

**A. 已完成（PR#1/#2，无需再做）**：Clerk 2 / 5 / 6，Clerk 1 主体，Clerk 7 的 Clerk 页

**B. 只需解答（0 或极小代码）**：前言 P0；Clerk 4；Bible 1（部分）；前言 P1（SOP 已渲染）

**C. 小改（直接做，各 <0.5 天，无需 research）**：
- Clerk 7 Muse 侧 profile 清洗（1 行，套 cleanProfileName）
- Clerk 0 xhslink 短链（已调研，方案明确）
- Clerk 3 回跳 404（已调研，方案明确）
- Poet 1 脚本归到选题下（UI 重组）

**D. 中型功能（设计+实现，单线程够）**：
- Muse 1 三功能
- Poet 2 写稿绑定对标 SOP（与 Muse 1② 复用）
- Clerk 1 「融合单条/爆款拆解」的 UI 收敛

**E. 需 deepresearch / workflow（Opus 4.8 + Fable 5）**：
- **前言 P2** 全站 design 系统 + dark mode —— 多方案设计探索 + 评审
- **Muse 2** 待处理 mind-map —— 新交互 UX，需设计 + 组件方案
- **Bible 2** 账号知识库 —— 跨层架构（schema/导入/注入），需设计 + 对抗式评审

> C+D 是常规 PR，可批量做。E 三项值得先做设计/架构轮（workflow 多方案→评审→再落地），避免直接写代码走偏。
