# Dependencies
node_modules/
.pnpm-store/
**/.venv/
__pycache__/
*.pyc
*.pyo

# Build output
.next/
dist/
build/
out/

# Cache
.turbo/
.cache/
.eslintcache
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local
!.env.example

# Trigger.dev
.trigger/

# Vercel
.vercel/

# Drizzle migrations are tracked in git (SQL + meta snapshots + journal)
# drizzle-kit uses _journal.json + snapshot files to compute diffs on next generate.

# IDE
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*

# Next.js auto-generated
next-env.d.ts

# Python sidecar build artifacts
*.egg-info/
.pytest_cache/

# Claude Code skills (restore via skills-lock.json + `npx skills install` then move from .agents/skills/ → .claude/skills/)
.agents/
.claude/skills/

# Local data dumps (xlsx exports, never commit)
*.xlsx
.vercel


# Proxy session backups (provider lists with credentials — synced to DB via seed-proxy-sessions)
residential-proxies-*.txt
proxy-backup-*.txt

# INC6 pre-contract surgical backups (prod data dumps)
packages/db/backups/
